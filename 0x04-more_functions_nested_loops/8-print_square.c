#include "main.h"
/**
 *print_square - prints a square
 *
 *@n: parameter
 *Return: returns nothing
 */

void print_square(int size)
{
int l, s;
if (n > 0)
{
for (l = 0; l < n; l++)
{
_putchar(35);
if (l == (n - 1))
{
continue;
}
}
_putchar('\n');
}

_putchar('\n');
}
