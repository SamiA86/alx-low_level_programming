#include "main.h"
/**
 * islower - checks if alphabet is lowercase
 *
 * Description: using the main function
 * this program prints "checks wheter it is lowercase or not
 * Return: 1 for lowercas 0 for else
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}

